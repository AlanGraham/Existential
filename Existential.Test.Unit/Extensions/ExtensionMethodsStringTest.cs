// <copyright file="ExtensionMethodsStringTest.cs" company="Gavin Greig">
//   Copyright (c) Dr. Gavin T.D. Greig, 2020.
// </copyright>

namespace Existential.Extensions.Test
{
    using Existential.Extensions;

    using NUnit.Framework;

    [TestFixture]
    public static class ExtensionMethodsStringTest
    {
        /*
        [TestCase("this should be in sentence Case.", "This should be in sentence case.")]
        [TestCase("contains a single character word.", "Contains a single character word.")]
        public static void ToSentenceCase_Works(string inBefore, string inExpected)
        {
            // Act
            string theResult = inBefore.ToSentenceCase();

            // Assert
            Assert.That(theResult, Is.EqualTo(inExpected));
        }

        [TestCase("this should be in start Case.", "This Should Be In Start Case.")]
        [TestCase("contains a single character word.", "Contains A Single Character Word.")]
        public static void ToStartCase_Works(string inBefore, string inExpected)
        {
            // Act
            string theResult = inBefore.ToStartCase();

            // Assert
            Assert.That(theResult, Is.EqualTo(inExpected));
        }
        */
    }
}
